<!-- 1b802627-2e32-42f4-8246-1c34ae268656 127d0f57-c847-4fde-b8ce-3abe2a4c3fea -->
# Plano

1. Preparar ambiente

- Criar a branch `feat/whatsapp-unico`.

2. Atualizar conteúdo com único número

- Ajustar `index-dev.html` para exibir/apontar apenas `+55 (11) 91675-1560`, substituindo links `wa.me`, `tel:` e textos.
- Trocar botões com `data-whatsapp-distributed` por âncoras diretas para o novo número.

3. Atualizar pipeline de build

- Garantir que `index-dev.html` não carregue `src/js/whatsapp-distribution.js`.
- Remover `src/js/whatsapp-distribution.js` das fontes em `package.json` e `build.js`.
- Rodar `npm run build:js` para gerar novo `bundle.min.js` sem o script removido.

4. Gerar artefatos

- Executar `node build.js` para gerar `index.html` a partir do `index-dev.html` atualizado.

### To-dos

- [ ] Criar a branch feat/whatsapp-unico
- [ ] Atualizar arquivos para usar somente +55 (11) 91675-1560
- [ ] Remover script de distribuição e ajustar build
- [ ] Gerar novo bundle e index.html